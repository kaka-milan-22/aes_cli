[project]
name = "encipherr-cli"
version = "1.3.3"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13.11"
dependencies = [
    "cryptography>=46,<47",
]
[project.scripts]
encipherr = "encipherr:main"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["encipherr"]

[tool.pytest.ini_options]
pythonpath = ["."]
